// Single source of truth for marketing + build versions.
// Bump MARKETING_VERSION for user-facing releases; bump CURRENT_PROJECT_VERSION every notarized build.
MARKETING_VERSION = 1.0.0
CURRENT_PROJECT_VERSION = 1
